I am now moving this application from DEVELOPMENT/TESTING to a clean PRODUCTION launch.

I want to completely remove the development mock-login/testing system and reset the application data so the platform starts from ZERO for real users.

IMPORTANT:
This is a production preparation task. Inspect the existing codebase and database structure carefully before making changes.

==================================================
1. REMOVE DEV MOCK LOGIN COMPLETELY
==================================================

Remove the development mock authentication system that was previously added.

Remove:

- "DEV MODE" UI
- "Development Test Login" section
- Customer test-login button
- Restaurant test-login button
- Driver test-login button
- Admin test-login button
- /api/auth/mock-login or equivalent mock-login endpoint
- mock authentication middleware/logic
- mock authentication environment variables if they are no longer needed
- test-user authentication shortcuts
- frontend mock authentication logic
- any special development authentication branches

The normal production login must remain.

The login page should return to the normal real-user flow:

Phone Number
+
Account Type
+
OTP verification

There must be NO way for a user to bypass OTP through the frontend.

There must also be NO mock-login endpoint available in production.

==================================================
2. REMOVE TEST USERS
==================================================

Find all test/development users created for the previous testing system.

Examples may include:

test-customer-001
test-restaurant-001
test-driver-001
test-admin-001

Remove those test users and any test-specific profile records associated with them.

Do NOT delete real users if any already exist.

Identify test records carefully before deleting them.

==================================================
3. RESET APPLICATION DATABASE
==================================================

I want the production application to start with a CLEAN DATABASE.

Remove testing/demo data from the application.

This includes, where applicable:

- test customers
- test restaurants
- test drivers
- test orders
- test deliveries
- test menus
- test products
- test carts
- test payments
- test notifications
- test addresses
- test reviews
- test messages
- test sessions
- test OTP records
- test verification records
- test uploaded test documents
- test/admin temporary records
- other development/demo data

IMPORTANT:

Do NOT destroy the DATABASE SCHEMA.

Do NOT remove tables.

Do NOT remove migrations.

Do NOT remove indexes.

Do NOT remove constraints.

Do NOT remove enums required by the application.

Only remove the existing DEVELOPMENT/TEST DATA.

The application must remain fully functional after the cleanup.

==================================================
4. DATABASE RESET SAFETY
==================================================

Before deleting anything, inspect the database relationships and foreign keys.

Respect the correct deletion order.

For example:

dependent records
↓
child records
↓
parent records

Do not leave orphaned records.

If the database uses PostgreSQL, use the existing ORM/database layer or a controlled SQL transaction where appropriate.

Do NOT blindly run:

DROP DATABASE

and do NOT blindly drop all tables.

The goal is:

EMPTY PRODUCTION DATA

NOT

EMPTY DATABASE SCHEMA.

==================================================
5. ADMIN ACCOUNT
==================================================

IMPORTANT:

Do NOT accidentally remove the real production administrator account if one already exists.

If there is currently only a development/test admin account, do NOT silently leave it as a production admin.

Instead, tell me exactly what is required to create the first real production administrator.

The application should have a secure way to create the initial production admin.

Do NOT create a publicly accessible "create admin" endpoint.

==================================================
6. AUTHENTICATION
==================================================

Production authentication must work exactly like the original intended system:

User selects account type
↓
Enters real mobile number
↓
OTP is sent
↓
User enters OTP
↓
Server verifies OTP
↓
Server authenticates the user
↓
Real session is created
↓
User enters the appropriate dashboard

Keep the existing secure session/cookie mechanism.

Do NOT replace OTP authentication with passwords or mock authentication.

==================================================
7. ACCOUNT TYPES
==================================================

Keep the existing real account types:

- Customer
- Restaurant
- Driver
- Admin

Account-type separation must remain enforced server-side.

A Customer must not be able to access Restaurant APIs.

A Restaurant must not be able to access Driver APIs.

A Driver must not be able to access Admin APIs.

An Admin should retain the existing administrative permissions.

Do not weaken authorization during this cleanup.

==================================================
8. SESSIONS
==================================================

Because this application is moving to production, remove all development/test sessions.

Invalidate old test sessions.

Make sure no previously authenticated test user remains logged in.

After cleanup, users should need to authenticate normally.

Verify that:

- logout works
- expired sessions are rejected
- unauthorized requests are rejected
- protected API endpoints require authentication
- role-based authorization still works

==================================================
9. OTP
==================================================

Make sure OTP functionality is production-ready.

Check:

- OTP generation
- OTP expiration
- OTP verification
- retry behavior
- rate limiting if already implemented
- invalid OTP handling
- expired OTP handling
- session creation after successful OTP
- account-type validation

Do NOT disable OTP.

Do NOT create a development OTP bypass.

Do NOT expose OTP values in production responses or frontend code.

==================================================
10. FRONTEND CLEANUP
==================================================

The login page must no longer show:

DEV MODE
TEST LOGIN
TEST CUSTOMER
TEST RESTAURANT
TEST DRIVER
TEST ADMIN

It should look like a normal production login page.

Keep the existing design and Arabic UI.

Do not redesign unrelated parts of the application.

==================================================
11. ENVIRONMENT VARIABLES
==================================================

Inspect the environment variables.

Remove development-only authentication variables such as:

MOCK_AUTH_ENABLED

if they are no longer used.

Make sure production-required environment variables are clearly identified.

Do NOT expose secrets in frontend code.

Do NOT commit secrets/API keys/passwords to the repository.

==================================================
12. STORAGE / UPLOADS
==================================================

If the application has uploaded test files/images/documents, identify development/test uploads and remove them where safe.

Do NOT delete production assets.

Do NOT delete the application logo or required static assets.

==================================================
13. PRODUCTION DATA POLICY
==================================================

After this cleanup:

There should be:

0 test customers
0 test restaurants
0 test drivers
0 test orders
0 test deliveries
0 test payments
0 test notifications
0 test sessions

unless a record is explicitly required as a system/configuration record.

The first real customer/restaurant/driver should be created through the normal registration/authentication flow.

==================================================
14. DO NOT MODIFY BUSINESS LOGIC
==================================================

Do NOT change:

- ordering logic
- restaurant logic
- driver logic
- delivery logic
- payment logic
- pricing
- commissions
- notifications
- dashboards
- API contracts
- database schema

unless a change is strictly required to remove the development mock-auth system.

This is a production cleanup, not a feature rewrite.

==================================================
15. PRODUCTION SECURITY CHECK
==================================================

After cleanup, verify that:

POST /api/auth/mock-login

or any equivalent endpoint:

- no longer exists OR
- is completely disabled and inaccessible in production.

Verify that manually calling the old mock-login endpoint cannot authenticate a user.

Verify that changing frontend JavaScript cannot grant a user a different role.

Verify that authorization is enforced on the server.

==================================================
16. FINAL VERIFICATION
==================================================

Before finishing, test:

1. New Customer registration/login using real OTP.
2. New Restaurant registration/login using real OTP.
3. New Driver registration/login using real OTP.
4. Admin authentication using the proper existing admin mechanism.
5. Logout.
6. Browser refresh while authenticated.
7. Accessing protected routes without authentication.
8. Trying to access another role's protected API.
9. Invalid OTP.
10. Expired OTP.

Do NOT send real SMS repeatedly during testing unless necessary. Use the existing production OTP provider correctly.

==================================================
17. DATABASE VERIFICATION
==================================================

After cleanup, verify the database state.

Report:

- number of remaining users
- number of remaining restaurants
- number of remaining drivers
- number of remaining orders
- number of remaining deliveries
- number of remaining sessions
- number of remaining OTP records

If any records remain, explain why they are system/configuration records and not test data.

==================================================
18. IMPORTANT FINAL REPORT
==================================================

When finished, give me a report containing:

1. Files changed
2. Mock-auth files/routes removed
3. Test users removed
4. Test data removed
5. Database tables affected
6. Database schema preserved confirmation
7. Current authentication flow
8. Current OTP flow
9. Current admin situation
10. Environment variables removed/required
11. Confirmation that old mock login cannot work
12. Confirmation that role authorization still works
13. Confirmation that the application is ready for production launch

DO NOT just hide the DEV MODE UI.

Actually remove the development authentication system and clean the test data from the database.

IMPORTANT:
Before executing destructive database operations, show me exactly what data you intend to delete and ask for confirmation if the operation could affect any non-test/production data.