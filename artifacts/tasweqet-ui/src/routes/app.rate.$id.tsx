import{useEffect as reactUseEffect,useState}from"react";import{createFileRoute}from"@tanstack/react-router";import{AppBar,Button,Card,Icon,MobileShell}from"@/components/tb/shell";import{customerTabs}from"@/lib/tb/nav";import{getToken}from"@/lib/auth-session";const useEffect=(fn:()=>unknown,deps:unknown[])=>reactUseEffect(()=>{void fn()},deps);
export const Route=createFileRoute("/app/rate/$id")({component:Rate});
type Eligibility={eligible:boolean;review:{rating:number;comment:string|null}|null;order:{id:number;restaurantName:string}};
function Rate(){const{id}=Route.useParams();const[data,setData]=useState<Eligibility>();const[error,setError]=useState("");const[rating,setRating]=useState(5);const[comment,setComment]=useState("");const load=()=>fetch(`/api/reviews/orders/${id}`,{headers:{Authorization:`Bearer ${getToken()}`}}).then(async r=>{const b=await r.json();if(!r.ok)throw new Error(b.error);setData(b)}).catch(e=>setError(e.message));useEffect(load,[id]);async function submit(){setError("");const r=await fetch(`/api/reviews/orders/${id}`,{method:"POST",headers:{Authorization:`Bearer ${getToken()}`,"Content-Type":"application/json"},body:JSON.stringify({rating,comment})});const b=await r.json();if(!r.ok){setError(b.error);return}load()}return <MobileShell tabs={customerTabs}><AppBar title="تقييم الطلب" back="/app/orders"/><div className="flex flex-col gap-md p-md">{error&&<Card className="p-md text-error">{error}</Card>}{!data?<Card className="p-md">جاري التحقق…</Card>:data.review?<Card className="p-md">تم إرسال تقييمك ({data.review.rating}/5). لا يمكن تقييم الطلب مرتين.</Card>:!data.eligible?<Card className="p-md">التقييم متاح بعد تسليم الطلب فقط.</Card>:<Card className="flex flex-col gap-md p-md"><h2>{data.order.restaurantName}</h2><div className="flex">{[1,2,3,4,5].map(n=><button key={n} onClick={()=>setRating(n)}><Icon name="star" filled={n<=rating}/></button>)}</div><textarea value={comment} onChange={e=>setComment(e.target.value)} maxLength={1000} className="rounded-button border p-3" placeholder="تعليق اختياري"/><Button onClick={submit}>إرسال التقييم</Button></Card>}</div></MobileShell>}